# OpenPQFormer Idea

Investigate whether unsupported spatial regions in medical image segmentation should be modeled as an explicit unknown-region or abstention map rather than being forced into a known-class prediction.

The current question is whether the literature support is strong enough to justify a paper framed around problem definition, uncertainty-aware segmentation, and medical OOD localization.
